--- src/app/core/services/patient.service.ts
+++ src/app/core/services/patient.service.ts
@@ -58,6 +58,7 @@
     const [patientsRes, apptRes, recordsRes] = await Promise.all([
       this.supabase
         .from('patient')
         .select('id, cpf, birth_date, gender, actor:actor!patient_id_fkey(name, clinic_id, created_at)')
         .eq('clinic_id', clinicId),
       
       this.supabase
         .from('appointment')
         .select('*, patient:patient_id(actor:actor!patient_id_fkey(name))')
         .eq('clinic_id', clinicId)
         .order('appointment_date', { ascending: true }),
         
       this.supabase
         .from('clinical_record')
         .select('*, patient:patient_id(actor:actor!patient_id_fkey(name))')
         .eq('clinic_id', clinicId)
         .order('timestamp', { ascending: false })
     ]);
+
+    console.log('patientsRes:', patientsRes.error);
+    console.log('apptRes:', apptRes.error);
+    console.log('recordsRes:', recordsRes.error);
+    console.log('apptData len:', apptRes.data?.length);
