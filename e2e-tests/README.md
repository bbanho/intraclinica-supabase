# IntraClinica E2E Tests
This workspace contains the official Playwright end-to-end test suite for the IntraClinica SaaS.
It is isolated from the main frontend node_modules to prevent EACCES permission locks.

## Usage
Run `npx playwright test` to execute the production verification suite.
